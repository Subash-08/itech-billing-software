'use client';
import {useState} from 'react';
import Link from 'next/link';
import {Plus,Pencil} from 'lucide-react';
import {ServiceItem} from '@/lib/extensions';
import {uid,money} from '@/lib/domain';
import {useStore} from './store';
import {PageHead,Card,SearchBox,Btn,Modal,Field,Badge,Empty} from './ui';
export default function ServiceCatalog(){const {state,setState,notify}=useStore();const [q,setQ]=useState(''),[form,setForm]=useState<ServiceItem|null>(null);const list=state.serviceCatalog.filter(s=>(s.name+s.category).toLowerCase().includes(q.toLowerCase()));return <><PageHead title="Service catalogue" description="Save common services and charges so billing takes only a few clicks." actions={<Btn onClick={()=>setForm({id:uid('SRV'),name:'',category:'Maintenance',description:'',rate:0,tax:18,sac:'998713',warranty:0,active:true})}><Plus size={16}/>Add service</Btn>}/><Card><div className="toolbar"><SearchBox value={q} onChange={setQ} placeholder="Search services…"/></div><div className="table-wrap"><table><thead><tr><th>Service</th><th>Category</th><th>Rate incl. GST</th><th>GST / SAC</th><th>Workmanship warranty</th><th>Status</th><th/></tr></thead><tbody>{list.map(s=><tr key={s.id}><td>{s.name}<small>{s.description}</small></td><td>{s.category}</td><td>{money(s.rate)}</td><td>{s.tax}%<small>{s.sac}</small></td><td>{s.warranty?`${s.warranty} month(s)`:'None'}</td><td><Badge>{s.active?'Active':'Inactive'}</Badge></td><td><button className="icon-btn" aria-label={'Edit '+s.name} onClick={()=>setForm({...s})}><Pencil size={16}/></button></td></tr>)}</tbody></table></div>{!list.length&&<Empty/>}</Card>{form&&<Modal title="Service details" onClose={()=>setForm(null)}><form onSubmit={e=>{e.preventDefault();setState(s=>({...s,serviceCatalog:s.serviceCatalog.some(x=>x.id===form.id)?s.serviceCatalog.map(x=>x.id===form.id?form:x):[...s.serviceCatalog,form]}));notify('Service saved. It is now available in invoice forms.');setForm(null);}}><div className="form-body form-grid"><Field label="Service name"><input required value={form.name} onChange={e=>setForm({...form,name:e.target.value})}/></Field><Field label="Category"><input required value={form.category} onChange={e=>setForm({...form,category:e.target.value})}/></Field><Field label="Standard rate including tax"><input required type="number" min="0" step="0.01" value={form.rate} onChange={e=>setForm({...form,rate:+e.target.value})}/></Field><Field label="GST rate %"><input required type="number" min="0" max="100" value={form.tax} onChange={e=>setForm({...form,tax:+e.target.value})}/></Field><Field label="SAC"><input value={form.sac} onChange={e=>setForm({...form,sac:e.target.value})}/></Field><Field label="Warranty months"><input required type="number" min="0" value={form.warranty} onChange={e=>setForm({...form,warranty:+e.target.value})}/></Field><div className="full"><Field label="Service description"><textarea value={form.description} onChange={e=>setForm({...form,description:e.target.value})}/></Field></div><label className="check-row"><input type="checkbox" checked={form.active} onChange={e=>setForm({...form,active:e.target.checked})}/>Available for billing</label></div><div className="form-actions"><Btn secondary onClick={()=>setForm(null)}>Cancel</Btn><Btn type="submit">Save service</Btn></div></form></Modal>}</>;}
